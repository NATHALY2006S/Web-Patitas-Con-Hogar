import Link from 'next/link';import { login,register } from '@/app/actions/auth'
export function AuthForm({mode,error,message}:{mode:'login'|'register';error?:string;message?:string}){const isLogin=mode==='login';return <div className="card mx-auto max-w-lg p-8"><h1 className="text-4xl font-black">{isLogin?'Bienvenido de nuevo':'Únete a la comunidad'}</h1><p className="mt-2 text-black/55">{isLogin?'Continúa ayudando a cambiar historias.':'Crea tu cuenta según la forma en que participarás.'}</p>{error&&<p className="mt-4 rounded-xl bg-red-100 p-3 text-sm text-red-700">{error}</p>}{message&&<p className="mt-4 rounded-xl bg-green-100 p-3 text-sm text-green-700">{message}</p>}<form action={isLogin?login:register} className="mt-7 space-y-4">{!isLogin&&<><div><label>Nombre completo o refugio</label><input name="full_name" required className="field"/></div><div><label>Ciudad</label><input name="city" required className="field"/></div><div><label>Tipo de cuenta</label><select name="role" className="field"><option value="adoptante">Adoptante</option><option value="refugio">Refugio</option></select></div></>}<div><label>Correo electrónico</label><input type="email" name="email" required className="field"/></div><div><label>Contraseña</label><input type="password" name="password" minLength={6} required className="field"/></div><button className="btn w-full">{isLogin?'Ingresar':'Crear cuenta'}</button></form><p className="mt-5 text-center text-sm">{isLogin?'¿No tienes cuenta? ':'¿Ya tienes cuenta? '}<Link className="font-bold text-leaf" href={isLogin?'/registro':'/login'}>{isLogin?'Regístrate':'Ingresa'}</Link></p></div>}
