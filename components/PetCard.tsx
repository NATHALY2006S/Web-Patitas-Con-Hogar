import Image from 'next/image';import Link from 'next/link';import type { Pet } from '@/types/database'
export function PetCard({pet}:{pet:Pet}){return <article className="card overflow-hidden"><div className="relative h-56"><Image src={pet.image_url||'/placeholder.svg'} alt={pet.name} fill className="object-cover"/></div><div className="p-5"><div className="mb-2 flex items-center justify-between"><h3 className="text-xl font-black">{pet.name}</h3><span className="rounded-full bg-sun/20 px-3 py-1 text-xs font-bold">{pet.status.replace('_',' ')}</span></div><p className="text-sm text-black/60">{pet.breed} · {pet.age} años · {pet.city}</p><p className="mt-3 line-clamp-2 text-sm">{pet.description}</p><Link href={`/mascotas/${pet.id}`} className="mt-5 inline-block font-bold text-leaf">Conocer su historia →</Link></div></article>}
