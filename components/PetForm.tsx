import type { Pet } from '@/types/database'
export function PetForm({action,pet}:{action:(fd:FormData)=>void|Promise<void>;pet?:Pet}){return <form action={action} className="card grid gap-4 p-7 md:grid-cols-2"><div><label>Nombre</label><input name="name" defaultValue={pet?.name} required className="field"/></div><div><label>Especie</label><select name="species" defaultValue={pet?.species||'perro'} className="field"><option value="perro">Perro</option><option value="gato">Gato</option></select></div><div><label>Raza</label><input name="breed" defaultValue={pet?.breed} required className="field"/></div><div><label>Edad aproximada</label><input type="number" min="0" max="30" name="age" defaultValue={pet?.age} required className="field"/></div><div><label>Ciudad</label><input name="city" defaultValue={pet?.city} required className="field"/></div><div><label>URL de la imagen</label><input type="url" name="image_url" defaultValue={pet?.image_url} required className="field"/></div>{pet&&<div><label>Estado</label><select name="status" defaultValue={pet.status} className="field"><option value="disponible">Disponible</option><option value="en_proceso">En proceso</option><option value="adoptado">Adoptado</option></select></div>}<div className="md:col-span-2"><label>Historia y personalidad</label><textarea name="description" defaultValue={pet?.description} minLength={30} required rows={5} className="field"/></div><button className="btn md:col-span-2">{pet?'Guardar cambios':'Publicar mascota'}</button></form>}
